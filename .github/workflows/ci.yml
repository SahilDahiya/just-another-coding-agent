name: CI

on:
  push:
  pull_request:

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - name: Check out code
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up uv
        uses: astral-sh/setup-uv@v5

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: uv sync --extra dev --extra test

      - name: Lint
        run: uv run ruff check .

      - name: Run tests
        run: uv run python -m pytest tests/contracts tests/e2e tests/evaluations

      - name: Run Go tests
        run: go test ./cmd/jaca ./internal/jaca/...

      - name: Build bundled wheel
        env:
          JACA_BUILD_TUI: "1"
        run: uv build --wheel

      - name: Verify bundled wheel contents
        run: python scripts/verify_bundled_wheel.py dist/*.whl
